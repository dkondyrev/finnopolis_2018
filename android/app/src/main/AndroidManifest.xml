<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.nsk.decentury.bonuses">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="Главная">
        </activity>
        <activity
            android:name=".StoresActivity"
            android:label="Магазин" />
        <activity
            android:name=".ProfileActivity"
            android:label="Профиль" />
        <activity
            android:name=".CartActivity"
            android:label="Корзина" />
        <activity
            android:name=".ExchangeActivity"
            android:label="Обмен токенов" />
        <activity
            android:name=".CardActivity"
            android:label="Карта" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".store.StoreMainActivity"
            android:label="Главная"></activity>
        <activity
            android:name=".store.StoreProfileActivity"
            android:label="Профиль"></activity>
        <activity
            android:name=".store.StoreScanActivity"
            android:label="Считать карту"></activity>
    </application>

</manifest>